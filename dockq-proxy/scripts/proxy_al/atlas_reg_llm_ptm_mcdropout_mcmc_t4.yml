name: proxy_mcmc
# compute_config: proxy_final_run_v2_v100_16gb_1gpu # You may specify `compute_config_id` or `cloud` instead
# cluster_env: proxy_final_run:6 # You may specify `build_id` instead
runtime_env:
  working_dir: "/home/ray/default/"
entrypoint: >
  cd dockq-proxy/src/proxy_al 
  && bash setup.sh 
  && /home/ray/micromamba/envs/proxy/bin/python exp5_bootstrap_mcmc_baseline.py
max_retries: 0

# anyscale job submit /home/ray/default/dockq-proxy/scripts/proxy_al/atlas_reg_llm_ptm_mcdropout_mcmc_t4.yml